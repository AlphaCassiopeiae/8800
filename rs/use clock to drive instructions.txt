modes:

run
    mem: panel = 1
    cpu: hlta = 0 

halt
    mem: pready = 0
    cpu: hlta = 1 (halted)
stop
    pready = 1 (front panel has control)

    single step
        pos edge of clock to trigger instr 

    single step
        pos edge of clock to trigger instr


examine



single step: 


deposit:
get addr from bus
get data from switches

output data on bus
mwr bring low
    assert panel
    cpu should release ouptut pins
    

wait for xrdy to go high

deassert mwr and output data


examine:
get addr from pins

assert jump opdcode on data


RIGHT now
MEMROY 
  data, addr only out for transactions seen
  xrdy output only when needed

CPU
    reads
        addr output ALWAYS
        data input

    writes
        data, addr output sometimes


front panel
    deposit
        addr input
        data ouput sometimes
    examine 
        addr output 

examine
    put data on bus
    issue clock
    cpu 


instead of panel determining run.
clk run does it

if clk but no panel
    memory reponds to mem requests


if clk but panel
    front panel responds to mem reads

deposit
    put data on bus
    detrigger panel
    mem responds
    reassert panel

examine
    put data on bus
    assert panel
    assert clk x3